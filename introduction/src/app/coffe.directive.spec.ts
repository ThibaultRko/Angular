import { CoffeDirective } from './coffe.directive';

describe('CoffeDirective', () => {
  it('should create an instance', () => {
    const directive = new CoffeDirective();
    expect(directive).toBeTruthy();
  });
});
